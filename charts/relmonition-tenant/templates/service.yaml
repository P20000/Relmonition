apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-server
  namespace: couple-{{ .Values.coupleId }}
spec:
  selector:
    app: relmonition
    tenant: {{ .Values.coupleId | quote }}
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP