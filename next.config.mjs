/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  devIndicators: {
    position: 'bottom-left', // move it out of the way, or set false to hide entirely
  },
};

export default nextConfig;
